<%= form_for(@employees_course) do |f| %>
  <% if @employees_course.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@employees_course.errors.count, "error") %> prohibited this employees_course from being saved:</h2>

      <ul>
      <% @employees_course.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
